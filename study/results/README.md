# Generated study results

This directory is reserved for locally generated benchmark evidence. Generated JSON, PNG, and JSONL files are ignored by Git.

Do not create placeholder rates or confidence intervals. A result belongs here only after the notebook runtime produces it, the mini-gate passes, and the artifact is reviewed. Raw provider responses remain private and must not be published.
